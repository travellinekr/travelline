import GuideClient from "./GuideClient";

export const metadata = {
    title: "사용 가이드 · Travelline",
    description: "화면 캡처로 보는 Travelline 핵심 사용법",
};

export default function GuidePage() {
    return <GuideClient />;
}
